// Generated by HostBridgeEmitter. Do not edit.
import Foundation
import InterpreterModels
import InterpreterVM

// MARK: - Swift.Dictionary<Foundation.Data,Foundation.Data>

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Foundation.Data>", as: Swift.Dictionary<Foundation.Data,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Bool_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Bool>", as: Swift.Dictionary<Foundation.Data,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Double_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Double>", as: Swift.Dictionary<Foundation.Data,Swift.Double>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Int_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Int>", as: Swift.Dictionary<Foundation.Data,Swift.Int>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_String_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.String>", as: Swift.Dictionary<Foundation.Data,Swift.String>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_UInt8_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.UInt8>", as: Swift.Dictionary<Foundation.Data,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Foundation_u002E_Data_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Foundation.Data>", as: Swift.Dictionary<Swift.Bool,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Bool_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Bool>", as: Swift.Dictionary<Swift.Bool,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Double_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Double>", as: Swift.Dictionary<Swift.Bool,Swift.Double>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Int_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Int>", as: Swift.Dictionary<Swift.Bool,Swift.Int>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_String_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.String>", as: Swift.Dictionary<Swift.Bool,Swift.String>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_UInt8_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.UInt8>", as: Swift.Dictionary<Swift.Bool,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Foundation_u002E_Data_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Foundation.Data>", as: Swift.Dictionary<Swift.Double,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Bool_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Bool>", as: Swift.Dictionary<Swift.Double,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Double_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Double>", as: Swift.Dictionary<Swift.Double,Swift.Double>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Int_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Int>", as: Swift.Dictionary<Swift.Double,Swift.Int>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_String_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.String>", as: Swift.Dictionary<Swift.Double,Swift.String>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_UInt8_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.UInt8>", as: Swift.Dictionary<Swift.Double,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Foundation_u002E_Data_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Foundation.Data>", as: Swift.Dictionary<Swift.Int,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Bool_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Bool>", as: Swift.Dictionary<Swift.Int,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Double_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Double>", as: Swift.Dictionary<Swift.Int,Swift.Double>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Int_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Int>", as: Swift.Dictionary<Swift.Int,Swift.Int>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_String_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.String>", as: Swift.Dictionary<Swift.Int,Swift.String>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_UInt8_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.UInt8>", as: Swift.Dictionary<Swift.Int,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Foundation_u002E_Data_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Foundation.Data>", as: Swift.Dictionary<Swift.String,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Bool_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Bool>", as: Swift.Dictionary<Swift.String,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Double_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Double>", as: Swift.Dictionary<Swift.String,Swift.Double>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Int_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Int>", as: Swift.Dictionary<Swift.String,Swift.Int>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_String_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.String>", as: Swift.Dictionary<Swift.String,Swift.String>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_UInt8_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.UInt8>", as: Swift.Dictionary<Swift.String,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Foundation_u002E_Data_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Foundation.Data>", as: Swift.Dictionary<Swift.UInt8,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Bool_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Bool>", as: Swift.Dictionary<Swift.UInt8,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Double_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Double>", as: Swift.Dictionary<Swift.UInt8,Swift.Double>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Int_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Int>", as: Swift.Dictionary<Swift.UInt8,Swift.Int>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_String_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.String>", as: Swift.Dictionary<Swift.UInt8,Swift.String>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_UInt8_u003E__capacity_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.capacity expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.UInt8>", as: Swift.Dictionary<Swift.UInt8,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.capacity
    return .int(result)
}

public func HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__capacity(vm: inout VM, _ args: [Value]) throws -> Value {
    guard !args.isEmpty else { throw VMError.typeError("missing receiver") }
    let tid: TypeID
    switch args[0] {
    case .host(_, let receiverTid): tid = receiverTid
    case .metatype(let receiverTid): tid = receiverTid
    default: throw VMError.typeError("receiver must be host or metatype")
    }
    if tid == vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>") {
        switch args[0] {
        case .host: return try Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__capacity_instance(vm: &vm, args)
        default: break
        }
    }
    let receiverName = vm.debugTypeName(tid)
    let supported = [
    "  - Swift.Dictionary<Foundation.Data,Foundation.Data> ()",
] .joined(separator: "\n")
    throw VMError.typeError("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.capacity has no specialization for receiver \(receiverName). Supported specializations:\(supported)")
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Foundation.Data>", as: Swift.Dictionary<Foundation.Data,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Bool_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Bool>", as: Swift.Dictionary<Foundation.Data,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Double_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Double>", as: Swift.Dictionary<Foundation.Data,Swift.Double>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Int_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Int>", as: Swift.Dictionary<Foundation.Data,Swift.Int>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_String_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.String>", as: Swift.Dictionary<Foundation.Data,Swift.String>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_UInt8_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.UInt8>", as: Swift.Dictionary<Foundation.Data,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Foundation_u002E_Data_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Foundation.Data>", as: Swift.Dictionary<Swift.Bool,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Bool_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Bool>", as: Swift.Dictionary<Swift.Bool,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Double_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Double>", as: Swift.Dictionary<Swift.Bool,Swift.Double>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Int_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Int>", as: Swift.Dictionary<Swift.Bool,Swift.Int>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_String_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.String>", as: Swift.Dictionary<Swift.Bool,Swift.String>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_UInt8_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.UInt8>", as: Swift.Dictionary<Swift.Bool,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Foundation_u002E_Data_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Foundation.Data>", as: Swift.Dictionary<Swift.Double,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Bool_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Bool>", as: Swift.Dictionary<Swift.Double,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Double_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Double>", as: Swift.Dictionary<Swift.Double,Swift.Double>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Int_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Int>", as: Swift.Dictionary<Swift.Double,Swift.Int>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_String_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.String>", as: Swift.Dictionary<Swift.Double,Swift.String>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_UInt8_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.UInt8>", as: Swift.Dictionary<Swift.Double,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Foundation_u002E_Data_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Foundation.Data>", as: Swift.Dictionary<Swift.Int,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Bool_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Bool>", as: Swift.Dictionary<Swift.Int,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Double_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Double>", as: Swift.Dictionary<Swift.Int,Swift.Double>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Int_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Int>", as: Swift.Dictionary<Swift.Int,Swift.Int>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_String_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.String>", as: Swift.Dictionary<Swift.Int,Swift.String>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_UInt8_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.UInt8>", as: Swift.Dictionary<Swift.Int,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Foundation_u002E_Data_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Foundation.Data>", as: Swift.Dictionary<Swift.String,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Bool_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Bool>", as: Swift.Dictionary<Swift.String,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Double_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Double>", as: Swift.Dictionary<Swift.String,Swift.Double>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Int_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Int>", as: Swift.Dictionary<Swift.String,Swift.Int>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_String_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.String>", as: Swift.Dictionary<Swift.String,Swift.String>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_UInt8_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.UInt8>", as: Swift.Dictionary<Swift.String,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Foundation_u002E_Data_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Foundation.Data>", as: Swift.Dictionary<Swift.UInt8,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Bool_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Bool>", as: Swift.Dictionary<Swift.UInt8,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Double_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Double>", as: Swift.Dictionary<Swift.UInt8,Swift.Double>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Int_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Int>", as: Swift.Dictionary<Swift.UInt8,Swift.Int>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_String_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.String>", as: Swift.Dictionary<Swift.UInt8,Swift.String>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_UInt8_u003E__count_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.count expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.UInt8>", as: Swift.Dictionary<Swift.UInt8,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.count
    return .int(result)
}

public func HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__count(vm: inout VM, _ args: [Value]) throws -> Value {
    guard !args.isEmpty else { throw VMError.typeError("missing receiver") }
    let tid: TypeID
    switch args[0] {
    case .host(_, let receiverTid): tid = receiverTid
    case .metatype(let receiverTid): tid = receiverTid
    default: throw VMError.typeError("receiver must be host or metatype")
    }
    if tid == vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>") {
        switch args[0] {
        case .host: return try Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__count_instance(vm: &vm, args)
        default: break
        }
    }
    let receiverName = vm.debugTypeName(tid)
    let supported = [
    "  - Swift.Dictionary<Foundation.Data,Foundation.Data> ()",
] .joined(separator: "\n")
    throw VMError.typeError("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.count has no specialization for receiver \(receiverName). Supported specializations:\(supported)")
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Foundation.Data>", as: Swift.Dictionary<Foundation.Data,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Bool_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Bool>", as: Swift.Dictionary<Foundation.Data,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Double_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Double>", as: Swift.Dictionary<Foundation.Data,Swift.Double>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Int_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Int>", as: Swift.Dictionary<Foundation.Data,Swift.Int>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_String_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.String>", as: Swift.Dictionary<Foundation.Data,Swift.String>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_UInt8_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.UInt8>", as: Swift.Dictionary<Foundation.Data,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Foundation_u002E_Data_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Foundation.Data>", as: Swift.Dictionary<Swift.Bool,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Bool_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Bool>", as: Swift.Dictionary<Swift.Bool,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Double_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Double>", as: Swift.Dictionary<Swift.Bool,Swift.Double>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Int_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Int>", as: Swift.Dictionary<Swift.Bool,Swift.Int>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_String_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.String>", as: Swift.Dictionary<Swift.Bool,Swift.String>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_UInt8_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.UInt8>", as: Swift.Dictionary<Swift.Bool,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Foundation_u002E_Data_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Foundation.Data>", as: Swift.Dictionary<Swift.Double,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Bool_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Bool>", as: Swift.Dictionary<Swift.Double,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Double_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Double>", as: Swift.Dictionary<Swift.Double,Swift.Double>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Int_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Int>", as: Swift.Dictionary<Swift.Double,Swift.Int>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_String_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.String>", as: Swift.Dictionary<Swift.Double,Swift.String>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_UInt8_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.UInt8>", as: Swift.Dictionary<Swift.Double,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Foundation_u002E_Data_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Foundation.Data>", as: Swift.Dictionary<Swift.Int,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Bool_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Bool>", as: Swift.Dictionary<Swift.Int,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Double_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Double>", as: Swift.Dictionary<Swift.Int,Swift.Double>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Int_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Int>", as: Swift.Dictionary<Swift.Int,Swift.Int>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_String_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.String>", as: Swift.Dictionary<Swift.Int,Swift.String>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_UInt8_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.UInt8>", as: Swift.Dictionary<Swift.Int,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Foundation_u002E_Data_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Foundation.Data>", as: Swift.Dictionary<Swift.String,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Bool_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Bool>", as: Swift.Dictionary<Swift.String,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Double_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Double>", as: Swift.Dictionary<Swift.String,Swift.Double>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Int_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Int>", as: Swift.Dictionary<Swift.String,Swift.Int>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_String_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.String>", as: Swift.Dictionary<Swift.String,Swift.String>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_UInt8_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.UInt8>", as: Swift.Dictionary<Swift.String,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Foundation_u002E_Data_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Foundation.Data>", as: Swift.Dictionary<Swift.UInt8,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Bool_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Bool>", as: Swift.Dictionary<Swift.UInt8,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Double_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Double>", as: Swift.Dictionary<Swift.UInt8,Swift.Double>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Int_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Int>", as: Swift.Dictionary<Swift.UInt8,Swift.Int>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_String_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.String>", as: Swift.Dictionary<Swift.UInt8,Swift.String>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_UInt8_u003E__customMirror_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.customMirror expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.UInt8>", as: Swift.Dictionary<Swift.UInt8,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.customMirror
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.UInt8>"))
}

public func HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__customMirror(vm: inout VM, _ args: [Value]) throws -> Value {
    guard !args.isEmpty else { throw VMError.typeError("missing receiver") }
    let tid: TypeID
    switch args[0] {
    case .host(_, let receiverTid): tid = receiverTid
    case .metatype(let receiverTid): tid = receiverTid
    default: throw VMError.typeError("receiver must be host or metatype")
    }
    if tid == vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>") {
        switch args[0] {
        case .host: return try Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__customMirror_instance(vm: &vm, args)
        default: break
        }
    }
    let receiverName = vm.debugTypeName(tid)
    let supported = [
    "  - Swift.Dictionary<Foundation.Data,Foundation.Data> ()",
] .joined(separator: "\n")
    throw VMError.typeError("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.customMirror has no specialization for receiver \(receiverName). Supported specializations:\(supported)")
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Foundation.Data>", as: Swift.Dictionary<Foundation.Data,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Bool_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Bool>", as: Swift.Dictionary<Foundation.Data,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Double_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Double>", as: Swift.Dictionary<Foundation.Data,Swift.Double>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Int_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Int>", as: Swift.Dictionary<Foundation.Data,Swift.Int>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_String_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.String>", as: Swift.Dictionary<Foundation.Data,Swift.String>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_UInt8_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.UInt8>", as: Swift.Dictionary<Foundation.Data,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Foundation_u002E_Data_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Foundation.Data>", as: Swift.Dictionary<Swift.Bool,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Bool_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Bool>", as: Swift.Dictionary<Swift.Bool,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Double_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Double>", as: Swift.Dictionary<Swift.Bool,Swift.Double>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Int_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Int>", as: Swift.Dictionary<Swift.Bool,Swift.Int>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_String_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.String>", as: Swift.Dictionary<Swift.Bool,Swift.String>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_UInt8_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.UInt8>", as: Swift.Dictionary<Swift.Bool,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Foundation_u002E_Data_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Foundation.Data>", as: Swift.Dictionary<Swift.Double,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Bool_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Bool>", as: Swift.Dictionary<Swift.Double,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Double_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Double>", as: Swift.Dictionary<Swift.Double,Swift.Double>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Int_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Int>", as: Swift.Dictionary<Swift.Double,Swift.Int>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_String_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.String>", as: Swift.Dictionary<Swift.Double,Swift.String>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_UInt8_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.UInt8>", as: Swift.Dictionary<Swift.Double,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Foundation_u002E_Data_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Foundation.Data>", as: Swift.Dictionary<Swift.Int,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Bool_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Bool>", as: Swift.Dictionary<Swift.Int,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Double_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Double>", as: Swift.Dictionary<Swift.Int,Swift.Double>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Int_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Int>", as: Swift.Dictionary<Swift.Int,Swift.Int>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_String_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.String>", as: Swift.Dictionary<Swift.Int,Swift.String>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_UInt8_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.UInt8>", as: Swift.Dictionary<Swift.Int,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Foundation_u002E_Data_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Foundation.Data>", as: Swift.Dictionary<Swift.String,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Bool_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Bool>", as: Swift.Dictionary<Swift.String,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Double_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Double>", as: Swift.Dictionary<Swift.String,Swift.Double>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Int_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Int>", as: Swift.Dictionary<Swift.String,Swift.Int>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_String_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.String>", as: Swift.Dictionary<Swift.String,Swift.String>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_UInt8_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.UInt8>", as: Swift.Dictionary<Swift.String,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Foundation_u002E_Data_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Foundation.Data>", as: Swift.Dictionary<Swift.UInt8,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Bool_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Bool>", as: Swift.Dictionary<Swift.UInt8,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Double_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Double>", as: Swift.Dictionary<Swift.UInt8,Swift.Double>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Int_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Int>", as: Swift.Dictionary<Swift.UInt8,Swift.Int>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_String_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.String>", as: Swift.Dictionary<Swift.UInt8,Swift.String>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_UInt8_u003E__debugDescription_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.debugDescription expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.UInt8>", as: Swift.Dictionary<Swift.UInt8,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.debugDescription
    return .string(result)
}

public func HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__debugDescription(vm: inout VM, _ args: [Value]) throws -> Value {
    guard !args.isEmpty else { throw VMError.typeError("missing receiver") }
    let tid: TypeID
    switch args[0] {
    case .host(_, let receiverTid): tid = receiverTid
    case .metatype(let receiverTid): tid = receiverTid
    default: throw VMError.typeError("receiver must be host or metatype")
    }
    if tid == vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>") {
        switch args[0] {
        case .host: return try Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__debugDescription_instance(vm: &vm, args)
        default: break
        }
    }
    let receiverName = vm.debugTypeName(tid)
    let supported = [
    "  - Swift.Dictionary<Foundation.Data,Foundation.Data> ()",
] .joined(separator: "\n")
    throw VMError.typeError("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.debugDescription has no specialization for receiver \(receiverName). Supported specializations:\(supported)")
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Foundation.Data>", as: Swift.Dictionary<Foundation.Data,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Bool_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Bool>", as: Swift.Dictionary<Foundation.Data,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Double_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Double>", as: Swift.Dictionary<Foundation.Data,Swift.Double>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Int_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Int>", as: Swift.Dictionary<Foundation.Data,Swift.Int>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_String_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.String>", as: Swift.Dictionary<Foundation.Data,Swift.String>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_UInt8_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.UInt8>", as: Swift.Dictionary<Foundation.Data,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Foundation_u002E_Data_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Foundation.Data>", as: Swift.Dictionary<Swift.Bool,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Bool_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Bool>", as: Swift.Dictionary<Swift.Bool,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Double_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Double>", as: Swift.Dictionary<Swift.Bool,Swift.Double>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Int_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Int>", as: Swift.Dictionary<Swift.Bool,Swift.Int>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_String_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.String>", as: Swift.Dictionary<Swift.Bool,Swift.String>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_UInt8_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.UInt8>", as: Swift.Dictionary<Swift.Bool,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Foundation_u002E_Data_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Foundation.Data>", as: Swift.Dictionary<Swift.Double,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Bool_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Bool>", as: Swift.Dictionary<Swift.Double,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Double_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Double>", as: Swift.Dictionary<Swift.Double,Swift.Double>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Int_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Int>", as: Swift.Dictionary<Swift.Double,Swift.Int>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_String_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.String>", as: Swift.Dictionary<Swift.Double,Swift.String>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_UInt8_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.UInt8>", as: Swift.Dictionary<Swift.Double,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Foundation_u002E_Data_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Foundation.Data>", as: Swift.Dictionary<Swift.Int,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Bool_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Bool>", as: Swift.Dictionary<Swift.Int,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Double_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Double>", as: Swift.Dictionary<Swift.Int,Swift.Double>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Int_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Int>", as: Swift.Dictionary<Swift.Int,Swift.Int>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_String_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.String>", as: Swift.Dictionary<Swift.Int,Swift.String>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_UInt8_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.UInt8>", as: Swift.Dictionary<Swift.Int,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Foundation_u002E_Data_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Foundation.Data>", as: Swift.Dictionary<Swift.String,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Bool_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Bool>", as: Swift.Dictionary<Swift.String,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Double_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Double>", as: Swift.Dictionary<Swift.String,Swift.Double>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Int_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Int>", as: Swift.Dictionary<Swift.String,Swift.Int>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_String_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.String>", as: Swift.Dictionary<Swift.String,Swift.String>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_UInt8_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.UInt8>", as: Swift.Dictionary<Swift.String,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Foundation_u002E_Data_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Foundation.Data>", as: Swift.Dictionary<Swift.UInt8,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Bool_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Bool>", as: Swift.Dictionary<Swift.UInt8,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Double_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Double>", as: Swift.Dictionary<Swift.UInt8,Swift.Double>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Int_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Int>", as: Swift.Dictionary<Swift.UInt8,Swift.Int>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_String_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.String>", as: Swift.Dictionary<Swift.UInt8,Swift.String>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_UInt8_u003E__description_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.description expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.UInt8>", as: Swift.Dictionary<Swift.UInt8,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.description
    return .string(result)
}

public func HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__description(vm: inout VM, _ args: [Value]) throws -> Value {
    guard !args.isEmpty else { throw VMError.typeError("missing receiver") }
    let tid: TypeID
    switch args[0] {
    case .host(_, let receiverTid): tid = receiverTid
    case .metatype(let receiverTid): tid = receiverTid
    default: throw VMError.typeError("receiver must be host or metatype")
    }
    if tid == vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>") {
        switch args[0] {
        case .host: return try Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__description_instance(vm: &vm, args)
        default: break
        }
    }
    let receiverName = vm.debugTypeName(tid)
    let supported = [
    "  - Swift.Dictionary<Foundation.Data,Foundation.Data> ()",
] .joined(separator: "\n")
    throw VMError.typeError("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.description has no specialization for receiver \(receiverName). Supported specializations:\(supported)")
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Foundation.Data>", as: Swift.Dictionary<Foundation.Data,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Bool_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Bool>", as: Swift.Dictionary<Foundation.Data,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Double_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Double>", as: Swift.Dictionary<Foundation.Data,Swift.Double>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Int_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Int>", as: Swift.Dictionary<Foundation.Data,Swift.Int>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_String_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.String>", as: Swift.Dictionary<Foundation.Data,Swift.String>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_UInt8_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.UInt8>", as: Swift.Dictionary<Foundation.Data,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Foundation_u002E_Data_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Foundation.Data>", as: Swift.Dictionary<Swift.Bool,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Bool_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Bool>", as: Swift.Dictionary<Swift.Bool,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Double_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Double>", as: Swift.Dictionary<Swift.Bool,Swift.Double>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Int_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Int>", as: Swift.Dictionary<Swift.Bool,Swift.Int>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_String_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.String>", as: Swift.Dictionary<Swift.Bool,Swift.String>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_UInt8_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.UInt8>", as: Swift.Dictionary<Swift.Bool,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Foundation_u002E_Data_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Foundation.Data>", as: Swift.Dictionary<Swift.Double,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Bool_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Bool>", as: Swift.Dictionary<Swift.Double,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Double_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Double>", as: Swift.Dictionary<Swift.Double,Swift.Double>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Int_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Int>", as: Swift.Dictionary<Swift.Double,Swift.Int>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_String_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.String>", as: Swift.Dictionary<Swift.Double,Swift.String>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_UInt8_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.UInt8>", as: Swift.Dictionary<Swift.Double,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Foundation_u002E_Data_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Foundation.Data>", as: Swift.Dictionary<Swift.Int,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Bool_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Bool>", as: Swift.Dictionary<Swift.Int,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Double_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Double>", as: Swift.Dictionary<Swift.Int,Swift.Double>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Int_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Int>", as: Swift.Dictionary<Swift.Int,Swift.Int>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_String_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.String>", as: Swift.Dictionary<Swift.Int,Swift.String>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_UInt8_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.UInt8>", as: Swift.Dictionary<Swift.Int,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Foundation_u002E_Data_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Foundation.Data>", as: Swift.Dictionary<Swift.String,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Bool_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Bool>", as: Swift.Dictionary<Swift.String,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Double_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Double>", as: Swift.Dictionary<Swift.String,Swift.Double>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Int_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Int>", as: Swift.Dictionary<Swift.String,Swift.Int>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_String_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.String>", as: Swift.Dictionary<Swift.String,Swift.String>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_UInt8_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.UInt8>", as: Swift.Dictionary<Swift.String,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Foundation_u002E_Data_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Foundation.Data>", as: Swift.Dictionary<Swift.UInt8,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Bool_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Bool>", as: Swift.Dictionary<Swift.UInt8,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Double_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Double>", as: Swift.Dictionary<Swift.UInt8,Swift.Double>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Int_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Int>", as: Swift.Dictionary<Swift.UInt8,Swift.Int>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_String_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.String>", as: Swift.Dictionary<Swift.UInt8,Swift.String>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_UInt8_u003E__endIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.endIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.UInt8>", as: Swift.Dictionary<Swift.UInt8,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.endIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.UInt8>"))
}

public func HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__endIndex(vm: inout VM, _ args: [Value]) throws -> Value {
    guard !args.isEmpty else { throw VMError.typeError("missing receiver") }
    let tid: TypeID
    switch args[0] {
    case .host(_, let receiverTid): tid = receiverTid
    case .metatype(let receiverTid): tid = receiverTid
    default: throw VMError.typeError("receiver must be host or metatype")
    }
    if tid == vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>") {
        switch args[0] {
        case .host: return try Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__endIndex_instance(vm: &vm, args)
        default: break
        }
    }
    let receiverName = vm.debugTypeName(tid)
    let supported = [
    "  - Swift.Dictionary<Foundation.Data,Foundation.Data> ()",
] .joined(separator: "\n")
    throw VMError.typeError("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.endIndex has no specialization for receiver \(receiverName). Supported specializations:\(supported)")
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Foundation.Data>", as: Swift.Dictionary<Foundation.Data,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Bool_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Bool>", as: Swift.Dictionary<Foundation.Data,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Bool>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Double_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Double>", as: Swift.Dictionary<Foundation.Data,Swift.Double>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Double>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Int_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Int>", as: Swift.Dictionary<Foundation.Data,Swift.Int>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Int>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_String_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.String>", as: Swift.Dictionary<Foundation.Data,Swift.String>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.String>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_UInt8_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.UInt8>", as: Swift.Dictionary<Foundation.Data,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.UInt8>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Foundation_u002E_Data_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Foundation.Data>", as: Swift.Dictionary<Swift.Bool,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Foundation.Data>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Bool_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Bool>", as: Swift.Dictionary<Swift.Bool,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Bool>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Double_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Double>", as: Swift.Dictionary<Swift.Bool,Swift.Double>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Double>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Int_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Int>", as: Swift.Dictionary<Swift.Bool,Swift.Int>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Int>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_String_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.String>", as: Swift.Dictionary<Swift.Bool,Swift.String>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.String>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_UInt8_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.UInt8>", as: Swift.Dictionary<Swift.Bool,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.UInt8>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Foundation_u002E_Data_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Foundation.Data>", as: Swift.Dictionary<Swift.Double,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Double,Foundation.Data>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Bool_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Bool>", as: Swift.Dictionary<Swift.Double,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Bool>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Double_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Double>", as: Swift.Dictionary<Swift.Double,Swift.Double>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Double>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Int_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Int>", as: Swift.Dictionary<Swift.Double,Swift.Int>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Int>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_String_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.String>", as: Swift.Dictionary<Swift.Double,Swift.String>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.String>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_UInt8_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.UInt8>", as: Swift.Dictionary<Swift.Double,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.UInt8>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Foundation_u002E_Data_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Foundation.Data>", as: Swift.Dictionary<Swift.Int,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Int,Foundation.Data>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Bool_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Bool>", as: Swift.Dictionary<Swift.Int,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Bool>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Double_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Double>", as: Swift.Dictionary<Swift.Int,Swift.Double>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Double>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Int_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Int>", as: Swift.Dictionary<Swift.Int,Swift.Int>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Int>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_String_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.String>", as: Swift.Dictionary<Swift.Int,Swift.String>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.String>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_UInt8_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.UInt8>", as: Swift.Dictionary<Swift.Int,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.UInt8>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Foundation_u002E_Data_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Foundation.Data>", as: Swift.Dictionary<Swift.String,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.String,Foundation.Data>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Bool_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Bool>", as: Swift.Dictionary<Swift.String,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Bool>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Double_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Double>", as: Swift.Dictionary<Swift.String,Swift.Double>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Double>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Int_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Int>", as: Swift.Dictionary<Swift.String,Swift.Int>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Int>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_String_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.String>", as: Swift.Dictionary<Swift.String,Swift.String>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.String>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_UInt8_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.UInt8>", as: Swift.Dictionary<Swift.String,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.UInt8>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Foundation_u002E_Data_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Foundation.Data>", as: Swift.Dictionary<Swift.UInt8,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Foundation.Data>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Bool_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Bool>", as: Swift.Dictionary<Swift.UInt8,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Bool>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Double_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Double>", as: Swift.Dictionary<Swift.UInt8,Swift.Double>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Double>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Int_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Int>", as: Swift.Dictionary<Swift.UInt8,Swift.Int>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Int>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_String_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.String>", as: Swift.Dictionary<Swift.UInt8,Swift.String>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.String>"))
    case .none:
        return .nilValue
    }
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_UInt8_u003E__first_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.first expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.UInt8>", as: Swift.Dictionary<Swift.UInt8,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.first
    switch result {
    case .some(let unwrapped):
        return .host(HostRef(box: makeOpaqueBox(unwrapped)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.UInt8>"))
    case .none:
        return .nilValue
    }
}

public func HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__first(vm: inout VM, _ args: [Value]) throws -> Value {
    guard !args.isEmpty else { throw VMError.typeError("missing receiver") }
    let tid: TypeID
    switch args[0] {
    case .host(_, let receiverTid): tid = receiverTid
    case .metatype(let receiverTid): tid = receiverTid
    default: throw VMError.typeError("receiver must be host or metatype")
    }
    if tid == vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>") {
        switch args[0] {
        case .host: return try Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__first_instance(vm: &vm, args)
        default: break
        }
    }
    let receiverName = vm.debugTypeName(tid)
    let supported = [
    "  - Swift.Dictionary<Foundation.Data,Foundation.Data> ()",
] .joined(separator: "\n")
    throw VMError.typeError("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.first has no specialization for receiver \(receiverName). Supported specializations:\(supported)")
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Foundation.Data>", as: Swift.Dictionary<Foundation.Data,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Bool_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Bool>", as: Swift.Dictionary<Foundation.Data,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Double_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Double>", as: Swift.Dictionary<Foundation.Data,Swift.Double>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Int_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Int>", as: Swift.Dictionary<Foundation.Data,Swift.Int>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_String_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.String>", as: Swift.Dictionary<Foundation.Data,Swift.String>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_UInt8_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.UInt8>", as: Swift.Dictionary<Foundation.Data,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Foundation_u002E_Data_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Foundation.Data>", as: Swift.Dictionary<Swift.Bool,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Bool_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Bool>", as: Swift.Dictionary<Swift.Bool,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Double_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Double>", as: Swift.Dictionary<Swift.Bool,Swift.Double>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Int_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Int>", as: Swift.Dictionary<Swift.Bool,Swift.Int>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_String_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.String>", as: Swift.Dictionary<Swift.Bool,Swift.String>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_UInt8_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.UInt8>", as: Swift.Dictionary<Swift.Bool,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Foundation_u002E_Data_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Foundation.Data>", as: Swift.Dictionary<Swift.Double,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Bool_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Bool>", as: Swift.Dictionary<Swift.Double,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Double_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Double>", as: Swift.Dictionary<Swift.Double,Swift.Double>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Int_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Int>", as: Swift.Dictionary<Swift.Double,Swift.Int>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_String_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.String>", as: Swift.Dictionary<Swift.Double,Swift.String>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_UInt8_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.UInt8>", as: Swift.Dictionary<Swift.Double,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Foundation_u002E_Data_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Foundation.Data>", as: Swift.Dictionary<Swift.Int,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Bool_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Bool>", as: Swift.Dictionary<Swift.Int,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Double_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Double>", as: Swift.Dictionary<Swift.Int,Swift.Double>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Int_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Int>", as: Swift.Dictionary<Swift.Int,Swift.Int>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_String_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.String>", as: Swift.Dictionary<Swift.Int,Swift.String>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_UInt8_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.UInt8>", as: Swift.Dictionary<Swift.Int,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Foundation_u002E_Data_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Foundation.Data>", as: Swift.Dictionary<Swift.String,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Bool_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Bool>", as: Swift.Dictionary<Swift.String,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Double_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Double>", as: Swift.Dictionary<Swift.String,Swift.Double>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Int_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Int>", as: Swift.Dictionary<Swift.String,Swift.Int>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_String_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.String>", as: Swift.Dictionary<Swift.String,Swift.String>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_UInt8_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.UInt8>", as: Swift.Dictionary<Swift.String,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Foundation_u002E_Data_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Foundation.Data>", as: Swift.Dictionary<Swift.UInt8,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Bool_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Bool>", as: Swift.Dictionary<Swift.UInt8,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Double_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Double>", as: Swift.Dictionary<Swift.UInt8,Swift.Double>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Int_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Int>", as: Swift.Dictionary<Swift.UInt8,Swift.Int>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_String_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.String>", as: Swift.Dictionary<Swift.UInt8,Swift.String>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_UInt8_u003E__hashValue_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.hashValue expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.UInt8>", as: Swift.Dictionary<Swift.UInt8,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.hashValue
    return .int(result)
}

public func HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__hashValue(vm: inout VM, _ args: [Value]) throws -> Value {
    guard !args.isEmpty else { throw VMError.typeError("missing receiver") }
    let tid: TypeID
    switch args[0] {
    case .host(_, let receiverTid): tid = receiverTid
    case .metatype(let receiverTid): tid = receiverTid
    default: throw VMError.typeError("receiver must be host or metatype")
    }
    if tid == vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>") {
        switch args[0] {
        case .host: return try Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__hashValue_instance(vm: &vm, args)
        default: break
        }
    }
    let receiverName = vm.debugTypeName(tid)
    let supported = [
    "  - Swift.Dictionary<Foundation.Data,Foundation.Data> ()",
] .joined(separator: "\n")
    throw VMError.typeError("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.hashValue has no specialization for receiver \(receiverName). Supported specializations:\(supported)")
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Foundation.Data>", as: Swift.Dictionary<Foundation.Data,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Bool_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Bool>", as: Swift.Dictionary<Foundation.Data,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Double_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Double>", as: Swift.Dictionary<Foundation.Data,Swift.Double>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Int_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Int>", as: Swift.Dictionary<Foundation.Data,Swift.Int>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_String_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.String>", as: Swift.Dictionary<Foundation.Data,Swift.String>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_UInt8_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.UInt8>", as: Swift.Dictionary<Foundation.Data,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Foundation_u002E_Data_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Foundation.Data>", as: Swift.Dictionary<Swift.Bool,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Bool_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Bool>", as: Swift.Dictionary<Swift.Bool,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Double_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Double>", as: Swift.Dictionary<Swift.Bool,Swift.Double>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Int_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Int>", as: Swift.Dictionary<Swift.Bool,Swift.Int>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_String_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.String>", as: Swift.Dictionary<Swift.Bool,Swift.String>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_UInt8_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.UInt8>", as: Swift.Dictionary<Swift.Bool,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Foundation_u002E_Data_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Foundation.Data>", as: Swift.Dictionary<Swift.Double,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Bool_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Bool>", as: Swift.Dictionary<Swift.Double,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Double_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Double>", as: Swift.Dictionary<Swift.Double,Swift.Double>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Int_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Int>", as: Swift.Dictionary<Swift.Double,Swift.Int>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_String_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.String>", as: Swift.Dictionary<Swift.Double,Swift.String>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_UInt8_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.UInt8>", as: Swift.Dictionary<Swift.Double,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Foundation_u002E_Data_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Foundation.Data>", as: Swift.Dictionary<Swift.Int,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Bool_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Bool>", as: Swift.Dictionary<Swift.Int,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Double_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Double>", as: Swift.Dictionary<Swift.Int,Swift.Double>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Int_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Int>", as: Swift.Dictionary<Swift.Int,Swift.Int>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_String_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.String>", as: Swift.Dictionary<Swift.Int,Swift.String>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_UInt8_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.UInt8>", as: Swift.Dictionary<Swift.Int,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Foundation_u002E_Data_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Foundation.Data>", as: Swift.Dictionary<Swift.String,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Bool_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Bool>", as: Swift.Dictionary<Swift.String,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Double_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Double>", as: Swift.Dictionary<Swift.String,Swift.Double>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Int_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Int>", as: Swift.Dictionary<Swift.String,Swift.Int>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_String_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.String>", as: Swift.Dictionary<Swift.String,Swift.String>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_UInt8_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.UInt8>", as: Swift.Dictionary<Swift.String,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Foundation_u002E_Data_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Foundation.Data>", as: Swift.Dictionary<Swift.UInt8,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Bool_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Bool>", as: Swift.Dictionary<Swift.UInt8,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Double_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Double>", as: Swift.Dictionary<Swift.UInt8,Swift.Double>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Int_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Int>", as: Swift.Dictionary<Swift.UInt8,Swift.Int>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_String_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.String>", as: Swift.Dictionary<Swift.UInt8,Swift.String>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_UInt8_u003E__indices_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.indices expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.UInt8>", as: Swift.Dictionary<Swift.UInt8,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.indices
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.UInt8>"))
}

public func HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__indices(vm: inout VM, _ args: [Value]) throws -> Value {
    guard !args.isEmpty else { throw VMError.typeError("missing receiver") }
    let tid: TypeID
    switch args[0] {
    case .host(_, let receiverTid): tid = receiverTid
    case .metatype(let receiverTid): tid = receiverTid
    default: throw VMError.typeError("receiver must be host or metatype")
    }
    if tid == vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>") {
        switch args[0] {
        case .host: return try Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__indices_instance(vm: &vm, args)
        default: break
        }
    }
    let receiverName = vm.debugTypeName(tid)
    let supported = [
    "  - Swift.Dictionary<Foundation.Data,Foundation.Data> ()",
] .joined(separator: "\n")
    throw VMError.typeError("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.indices has no specialization for receiver \(receiverName). Supported specializations:\(supported)")
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Foundation.Data>", as: Swift.Dictionary<Foundation.Data,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Bool_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Bool>", as: Swift.Dictionary<Foundation.Data,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Double_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Double>", as: Swift.Dictionary<Foundation.Data,Swift.Double>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Int_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Int>", as: Swift.Dictionary<Foundation.Data,Swift.Int>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_String_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.String>", as: Swift.Dictionary<Foundation.Data,Swift.String>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_UInt8_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.UInt8>", as: Swift.Dictionary<Foundation.Data,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Foundation_u002E_Data_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Foundation.Data>", as: Swift.Dictionary<Swift.Bool,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Bool_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Bool>", as: Swift.Dictionary<Swift.Bool,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Double_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Double>", as: Swift.Dictionary<Swift.Bool,Swift.Double>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Int_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Int>", as: Swift.Dictionary<Swift.Bool,Swift.Int>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_String_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.String>", as: Swift.Dictionary<Swift.Bool,Swift.String>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_UInt8_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.UInt8>", as: Swift.Dictionary<Swift.Bool,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Foundation_u002E_Data_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Foundation.Data>", as: Swift.Dictionary<Swift.Double,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Bool_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Bool>", as: Swift.Dictionary<Swift.Double,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Double_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Double>", as: Swift.Dictionary<Swift.Double,Swift.Double>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Int_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Int>", as: Swift.Dictionary<Swift.Double,Swift.Int>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_String_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.String>", as: Swift.Dictionary<Swift.Double,Swift.String>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_UInt8_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.UInt8>", as: Swift.Dictionary<Swift.Double,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Foundation_u002E_Data_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Foundation.Data>", as: Swift.Dictionary<Swift.Int,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Bool_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Bool>", as: Swift.Dictionary<Swift.Int,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Double_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Double>", as: Swift.Dictionary<Swift.Int,Swift.Double>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Int_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Int>", as: Swift.Dictionary<Swift.Int,Swift.Int>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_String_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.String>", as: Swift.Dictionary<Swift.Int,Swift.String>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_UInt8_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.UInt8>", as: Swift.Dictionary<Swift.Int,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Foundation_u002E_Data_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Foundation.Data>", as: Swift.Dictionary<Swift.String,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Bool_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Bool>", as: Swift.Dictionary<Swift.String,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Double_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Double>", as: Swift.Dictionary<Swift.String,Swift.Double>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Int_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Int>", as: Swift.Dictionary<Swift.String,Swift.Int>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_String_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.String>", as: Swift.Dictionary<Swift.String,Swift.String>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_UInt8_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.UInt8>", as: Swift.Dictionary<Swift.String,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Foundation_u002E_Data_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Foundation.Data>", as: Swift.Dictionary<Swift.UInt8,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Bool_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Bool>", as: Swift.Dictionary<Swift.UInt8,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Double_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Double>", as: Swift.Dictionary<Swift.UInt8,Swift.Double>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Int_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Int>", as: Swift.Dictionary<Swift.UInt8,Swift.Int>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_String_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.String>", as: Swift.Dictionary<Swift.UInt8,Swift.String>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_UInt8_u003E__isEmpty_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.isEmpty expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.UInt8>", as: Swift.Dictionary<Swift.UInt8,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.isEmpty
    return .bool(result)
}

public func HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__isEmpty(vm: inout VM, _ args: [Value]) throws -> Value {
    guard !args.isEmpty else { throw VMError.typeError("missing receiver") }
    let tid: TypeID
    switch args[0] {
    case .host(_, let receiverTid): tid = receiverTid
    case .metatype(let receiverTid): tid = receiverTid
    default: throw VMError.typeError("receiver must be host or metatype")
    }
    if tid == vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>") {
        switch args[0] {
        case .host: return try Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__isEmpty_instance(vm: &vm, args)
        default: break
        }
    }
    let receiverName = vm.debugTypeName(tid)
    let supported = [
    "  - Swift.Dictionary<Foundation.Data,Foundation.Data> ()",
] .joined(separator: "\n")
    throw VMError.typeError("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.isEmpty has no specialization for receiver \(receiverName). Supported specializations:\(supported)")
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Foundation.Data>", as: Swift.Dictionary<Foundation.Data,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Bool_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Bool>", as: Swift.Dictionary<Foundation.Data,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Bool>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Double_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Double>", as: Swift.Dictionary<Foundation.Data,Swift.Double>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Double>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Int_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Int>", as: Swift.Dictionary<Foundation.Data,Swift.Int>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Int>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_String_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.String>", as: Swift.Dictionary<Foundation.Data,Swift.String>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.String>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_UInt8_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.UInt8>", as: Swift.Dictionary<Foundation.Data,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.UInt8>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Foundation_u002E_Data_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Foundation.Data>", as: Swift.Dictionary<Swift.Bool,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Foundation.Data>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Bool_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Bool>", as: Swift.Dictionary<Swift.Bool,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Bool>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Double_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Double>", as: Swift.Dictionary<Swift.Bool,Swift.Double>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Double>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Int_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Int>", as: Swift.Dictionary<Swift.Bool,Swift.Int>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Int>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_String_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.String>", as: Swift.Dictionary<Swift.Bool,Swift.String>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.String>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_UInt8_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.UInt8>", as: Swift.Dictionary<Swift.Bool,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.UInt8>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Foundation_u002E_Data_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Foundation.Data>", as: Swift.Dictionary<Swift.Double,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Foundation.Data>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Bool_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Bool>", as: Swift.Dictionary<Swift.Double,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Bool>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Double_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Double>", as: Swift.Dictionary<Swift.Double,Swift.Double>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Double>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Int_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Int>", as: Swift.Dictionary<Swift.Double,Swift.Int>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Int>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_String_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.String>", as: Swift.Dictionary<Swift.Double,Swift.String>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.String>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_UInt8_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.UInt8>", as: Swift.Dictionary<Swift.Double,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.UInt8>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Foundation_u002E_Data_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Foundation.Data>", as: Swift.Dictionary<Swift.Int,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Foundation.Data>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Bool_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Bool>", as: Swift.Dictionary<Swift.Int,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Bool>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Double_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Double>", as: Swift.Dictionary<Swift.Int,Swift.Double>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Double>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Int_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Int>", as: Swift.Dictionary<Swift.Int,Swift.Int>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Int>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_String_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.String>", as: Swift.Dictionary<Swift.Int,Swift.String>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.String>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_UInt8_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.UInt8>", as: Swift.Dictionary<Swift.Int,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.UInt8>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Foundation_u002E_Data_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Foundation.Data>", as: Swift.Dictionary<Swift.String,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Foundation.Data>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Bool_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Bool>", as: Swift.Dictionary<Swift.String,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Bool>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Double_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Double>", as: Swift.Dictionary<Swift.String,Swift.Double>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Double>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Int_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Int>", as: Swift.Dictionary<Swift.String,Swift.Int>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Int>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_String_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.String>", as: Swift.Dictionary<Swift.String,Swift.String>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.String>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_UInt8_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.UInt8>", as: Swift.Dictionary<Swift.String,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.UInt8>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Foundation_u002E_Data_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Foundation.Data>", as: Swift.Dictionary<Swift.UInt8,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Foundation.Data>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Bool_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Bool>", as: Swift.Dictionary<Swift.UInt8,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Bool>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Double_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Double>", as: Swift.Dictionary<Swift.UInt8,Swift.Double>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Double>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Int_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Int>", as: Swift.Dictionary<Swift.UInt8,Swift.Int>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Int>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_String_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.String>", as: Swift.Dictionary<Swift.UInt8,Swift.String>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.String>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_UInt8_u003E__keys_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.keys expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.UInt8>", as: Swift.Dictionary<Swift.UInt8,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.keys
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.UInt8>"))
}

@available(swift 4.0)
public func HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__keys(vm: inout VM, _ args: [Value]) throws -> Value {
    guard !args.isEmpty else { throw VMError.typeError("missing receiver") }
    let tid: TypeID
    switch args[0] {
    case .host(_, let receiverTid): tid = receiverTid
    case .metatype(let receiverTid): tid = receiverTid
    default: throw VMError.typeError("receiver must be host or metatype")
    }
    if tid == vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>") {
        switch args[0] {
        case .host:
            if true { return try Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__keys_instance(vm: &vm, args) }
            else { throw VMError.unsupported("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.keys is not available on this platform") }
        default: break
        }
    }
    let receiverName = vm.debugTypeName(tid)
    let supported = [
    "  - Swift.Dictionary<Foundation.Data,Foundation.Data> ()",
] .joined(separator: "\n")
    throw VMError.typeError("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.keys has no specialization for receiver \(receiverName). Supported specializations:\(supported)")
}

public func HostAvailWrap_HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__keys(vm: inout VM, _ args: [Value]) throws -> Value {
    if true { return try HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__keys(vm: &vm, args) }
    throw VMError.unsupported("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.keys is not available on this platform")
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Foundation.Data>", as: Swift.Dictionary<Foundation.Data,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Bool_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Bool>", as: Swift.Dictionary<Foundation.Data,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Double_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Double>", as: Swift.Dictionary<Foundation.Data,Swift.Double>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Int_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Int>", as: Swift.Dictionary<Foundation.Data,Swift.Int>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_String_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.String>", as: Swift.Dictionary<Foundation.Data,Swift.String>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_UInt8_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.UInt8>", as: Swift.Dictionary<Foundation.Data,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Foundation_u002E_Data_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Foundation.Data>", as: Swift.Dictionary<Swift.Bool,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Bool_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Bool>", as: Swift.Dictionary<Swift.Bool,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Double_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Double>", as: Swift.Dictionary<Swift.Bool,Swift.Double>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Int_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Int>", as: Swift.Dictionary<Swift.Bool,Swift.Int>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_String_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.String>", as: Swift.Dictionary<Swift.Bool,Swift.String>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_UInt8_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.UInt8>", as: Swift.Dictionary<Swift.Bool,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Foundation_u002E_Data_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Foundation.Data>", as: Swift.Dictionary<Swift.Double,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Bool_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Bool>", as: Swift.Dictionary<Swift.Double,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Double_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Double>", as: Swift.Dictionary<Swift.Double,Swift.Double>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Int_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Int>", as: Swift.Dictionary<Swift.Double,Swift.Int>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_String_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.String>", as: Swift.Dictionary<Swift.Double,Swift.String>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_UInt8_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.UInt8>", as: Swift.Dictionary<Swift.Double,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Foundation_u002E_Data_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Foundation.Data>", as: Swift.Dictionary<Swift.Int,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Bool_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Bool>", as: Swift.Dictionary<Swift.Int,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Double_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Double>", as: Swift.Dictionary<Swift.Int,Swift.Double>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Int_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Int>", as: Swift.Dictionary<Swift.Int,Swift.Int>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_String_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.String>", as: Swift.Dictionary<Swift.Int,Swift.String>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_UInt8_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.UInt8>", as: Swift.Dictionary<Swift.Int,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Foundation_u002E_Data_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Foundation.Data>", as: Swift.Dictionary<Swift.String,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Bool_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Bool>", as: Swift.Dictionary<Swift.String,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Double_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Double>", as: Swift.Dictionary<Swift.String,Swift.Double>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Int_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Int>", as: Swift.Dictionary<Swift.String,Swift.Int>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_String_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.String>", as: Swift.Dictionary<Swift.String,Swift.String>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_UInt8_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.UInt8>", as: Swift.Dictionary<Swift.String,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Foundation_u002E_Data_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Foundation.Data>", as: Swift.Dictionary<Swift.UInt8,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Bool_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Bool>", as: Swift.Dictionary<Swift.UInt8,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Double_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Double>", as: Swift.Dictionary<Swift.UInt8,Swift.Double>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Int_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Int>", as: Swift.Dictionary<Swift.UInt8,Swift.Int>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_String_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.String>", as: Swift.Dictionary<Swift.UInt8,Swift.String>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_UInt8_u003E__lazy_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.lazy expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.UInt8>", as: Swift.Dictionary<Swift.UInt8,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.lazy
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.UInt8>"))
}

public func HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__lazy(vm: inout VM, _ args: [Value]) throws -> Value {
    guard !args.isEmpty else { throw VMError.typeError("missing receiver") }
    let tid: TypeID
    switch args[0] {
    case .host(_, let receiverTid): tid = receiverTid
    case .metatype(let receiverTid): tid = receiverTid
    default: throw VMError.typeError("receiver must be host or metatype")
    }
    if tid == vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>") {
        switch args[0] {
        case .host: return try Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__lazy_instance(vm: &vm, args)
        default: break
        }
    }
    let receiverName = vm.debugTypeName(tid)
    let supported = [
    "  - Swift.Dictionary<Foundation.Data,Foundation.Data> ()",
] .joined(separator: "\n")
    throw VMError.typeError("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.lazy has no specialization for receiver \(receiverName). Supported specializations:\(supported)")
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Foundation.Data>", as: Swift.Dictionary<Foundation.Data,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Bool_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Bool>", as: Swift.Dictionary<Foundation.Data,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Double_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Double>", as: Swift.Dictionary<Foundation.Data,Swift.Double>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Int_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Int>", as: Swift.Dictionary<Foundation.Data,Swift.Int>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_String_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.String>", as: Swift.Dictionary<Foundation.Data,Swift.String>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_UInt8_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.UInt8>", as: Swift.Dictionary<Foundation.Data,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Foundation_u002E_Data_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Foundation.Data>", as: Swift.Dictionary<Swift.Bool,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Bool_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Bool>", as: Swift.Dictionary<Swift.Bool,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Double_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Double>", as: Swift.Dictionary<Swift.Bool,Swift.Double>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Int_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Int>", as: Swift.Dictionary<Swift.Bool,Swift.Int>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_String_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.String>", as: Swift.Dictionary<Swift.Bool,Swift.String>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_UInt8_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.UInt8>", as: Swift.Dictionary<Swift.Bool,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Foundation_u002E_Data_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Foundation.Data>", as: Swift.Dictionary<Swift.Double,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Bool_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Bool>", as: Swift.Dictionary<Swift.Double,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Double_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Double>", as: Swift.Dictionary<Swift.Double,Swift.Double>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Int_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Int>", as: Swift.Dictionary<Swift.Double,Swift.Int>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_String_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.String>", as: Swift.Dictionary<Swift.Double,Swift.String>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_UInt8_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.UInt8>", as: Swift.Dictionary<Swift.Double,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Foundation_u002E_Data_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Foundation.Data>", as: Swift.Dictionary<Swift.Int,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Bool_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Bool>", as: Swift.Dictionary<Swift.Int,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Double_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Double>", as: Swift.Dictionary<Swift.Int,Swift.Double>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Int_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Int>", as: Swift.Dictionary<Swift.Int,Swift.Int>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_String_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.String>", as: Swift.Dictionary<Swift.Int,Swift.String>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_UInt8_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.UInt8>", as: Swift.Dictionary<Swift.Int,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Foundation_u002E_Data_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Foundation.Data>", as: Swift.Dictionary<Swift.String,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Bool_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Bool>", as: Swift.Dictionary<Swift.String,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Double_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Double>", as: Swift.Dictionary<Swift.String,Swift.Double>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Int_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Int>", as: Swift.Dictionary<Swift.String,Swift.Int>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_String_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.String>", as: Swift.Dictionary<Swift.String,Swift.String>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_UInt8_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.UInt8>", as: Swift.Dictionary<Swift.String,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.UInt8>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Foundation_u002E_Data_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Foundation.Data>", as: Swift.Dictionary<Swift.UInt8,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Foundation.Data>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Bool_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Bool>", as: Swift.Dictionary<Swift.UInt8,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Bool>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Double_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Double>", as: Swift.Dictionary<Swift.UInt8,Swift.Double>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Double>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Int_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Int>", as: Swift.Dictionary<Swift.UInt8,Swift.Int>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Int>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_String_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.String>", as: Swift.Dictionary<Swift.UInt8,Swift.String>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.String>"))
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_UInt8_u003E__startIndex_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.startIndex expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.UInt8>", as: Swift.Dictionary<Swift.UInt8,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.startIndex
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.UInt8>"))
}

public func HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__startIndex(vm: inout VM, _ args: [Value]) throws -> Value {
    guard !args.isEmpty else { throw VMError.typeError("missing receiver") }
    let tid: TypeID
    switch args[0] {
    case .host(_, let receiverTid): tid = receiverTid
    case .metatype(let receiverTid): tid = receiverTid
    default: throw VMError.typeError("receiver must be host or metatype")
    }
    if tid == vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>") {
        switch args[0] {
        case .host: return try Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__startIndex_instance(vm: &vm, args)
        default: break
        }
    }
    let receiverName = vm.debugTypeName(tid)
    let supported = [
    "  - Swift.Dictionary<Foundation.Data,Foundation.Data> ()",
] .joined(separator: "\n")
    throw VMError.typeError("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.startIndex has no specialization for receiver \(receiverName). Supported specializations:\(supported)")
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Foundation.Data>", as: Swift.Dictionary<Foundation.Data,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Bool_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Bool>", as: Swift.Dictionary<Foundation.Data,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Double_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Double>", as: Swift.Dictionary<Foundation.Data,Swift.Double>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Int_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Int>", as: Swift.Dictionary<Foundation.Data,Swift.Int>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_String_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.String>", as: Swift.Dictionary<Foundation.Data,Swift.String>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_UInt8_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.UInt8>", as: Swift.Dictionary<Foundation.Data,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Foundation_u002E_Data_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Foundation.Data>", as: Swift.Dictionary<Swift.Bool,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Bool_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Bool>", as: Swift.Dictionary<Swift.Bool,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Double_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Double>", as: Swift.Dictionary<Swift.Bool,Swift.Double>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Int_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Int>", as: Swift.Dictionary<Swift.Bool,Swift.Int>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_String_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.String>", as: Swift.Dictionary<Swift.Bool,Swift.String>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_UInt8_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.UInt8>", as: Swift.Dictionary<Swift.Bool,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Foundation_u002E_Data_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Foundation.Data>", as: Swift.Dictionary<Swift.Double,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Bool_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Bool>", as: Swift.Dictionary<Swift.Double,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Double_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Double>", as: Swift.Dictionary<Swift.Double,Swift.Double>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Int_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Int>", as: Swift.Dictionary<Swift.Double,Swift.Int>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_String_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.String>", as: Swift.Dictionary<Swift.Double,Swift.String>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_UInt8_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.UInt8>", as: Swift.Dictionary<Swift.Double,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Foundation_u002E_Data_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Foundation.Data>", as: Swift.Dictionary<Swift.Int,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Bool_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Bool>", as: Swift.Dictionary<Swift.Int,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Double_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Double>", as: Swift.Dictionary<Swift.Int,Swift.Double>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Int_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Int>", as: Swift.Dictionary<Swift.Int,Swift.Int>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_String_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.String>", as: Swift.Dictionary<Swift.Int,Swift.String>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_UInt8_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.UInt8>", as: Swift.Dictionary<Swift.Int,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Foundation_u002E_Data_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Foundation.Data>", as: Swift.Dictionary<Swift.String,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Bool_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Bool>", as: Swift.Dictionary<Swift.String,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Double_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Double>", as: Swift.Dictionary<Swift.String,Swift.Double>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Int_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Int>", as: Swift.Dictionary<Swift.String,Swift.Int>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_String_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.String>", as: Swift.Dictionary<Swift.String,Swift.String>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_UInt8_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.UInt8>", as: Swift.Dictionary<Swift.String,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Foundation_u002E_Data_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Foundation.Data>", as: Swift.Dictionary<Swift.UInt8,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Bool_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Bool>", as: Swift.Dictionary<Swift.UInt8,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Double_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Double>", as: Swift.Dictionary<Swift.UInt8,Swift.Double>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Int_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Int>", as: Swift.Dictionary<Swift.UInt8,Swift.Int>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_String_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.String>", as: Swift.Dictionary<Swift.UInt8,Swift.String>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_UInt8_u003E__underestimatedCount_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.underestimatedCount expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.UInt8>", as: Swift.Dictionary<Swift.UInt8,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.underestimatedCount
    return .int(result)
}

public func HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__underestimatedCount(vm: inout VM, _ args: [Value]) throws -> Value {
    guard !args.isEmpty else { throw VMError.typeError("missing receiver") }
    let tid: TypeID
    switch args[0] {
    case .host(_, let receiverTid): tid = receiverTid
    case .metatype(let receiverTid): tid = receiverTid
    default: throw VMError.typeError("receiver must be host or metatype")
    }
    if tid == vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>") {
        switch args[0] {
        case .host: return try Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__underestimatedCount_instance(vm: &vm, args)
        default: break
        }
    }
    let receiverName = vm.debugTypeName(tid)
    let supported = [
    "  - Swift.Dictionary<Foundation.Data,Foundation.Data> ()",
] .joined(separator: "\n")
    throw VMError.typeError("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.underestimatedCount has no specialization for receiver \(receiverName). Supported specializations:\(supported)")
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Foundation.Data>", as: Swift.Dictionary<Foundation.Data,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Bool_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Bool>", as: Swift.Dictionary<Foundation.Data,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Bool>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Double_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Double>", as: Swift.Dictionary<Foundation.Data,Swift.Double>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Double>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_Int_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.Int>", as: Swift.Dictionary<Foundation.Data,Swift.Int>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.Int>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_String_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.String>", as: Swift.Dictionary<Foundation.Data,Swift.String>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.String>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Swift_u002E_UInt8_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Foundation.Data,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Foundation.Data,Swift.UInt8>", as: Swift.Dictionary<Foundation.Data,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Foundation.Data,Swift.UInt8>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Foundation_u002E_Data_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Foundation.Data>", as: Swift.Dictionary<Swift.Bool,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Foundation.Data>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Bool_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Bool>", as: Swift.Dictionary<Swift.Bool,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Bool>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Double_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Double>", as: Swift.Dictionary<Swift.Bool,Swift.Double>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Double>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_Int_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.Int>", as: Swift.Dictionary<Swift.Bool,Swift.Int>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.Int>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_String_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.String>", as: Swift.Dictionary<Swift.Bool,Swift.String>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.String>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Bool_u002C_Swift_u002E_UInt8_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Bool,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Bool,Swift.UInt8>", as: Swift.Dictionary<Swift.Bool,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Bool,Swift.UInt8>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Foundation_u002E_Data_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Foundation.Data>", as: Swift.Dictionary<Swift.Double,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Foundation.Data>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Bool_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Bool>", as: Swift.Dictionary<Swift.Double,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Bool>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Double_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Double>", as: Swift.Dictionary<Swift.Double,Swift.Double>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Double>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_Int_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.Int>", as: Swift.Dictionary<Swift.Double,Swift.Int>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.Int>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_String_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.String>", as: Swift.Dictionary<Swift.Double,Swift.String>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.String>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Double_u002C_Swift_u002E_UInt8_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Double,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Double,Swift.UInt8>", as: Swift.Dictionary<Swift.Double,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Double,Swift.UInt8>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Foundation_u002E_Data_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Foundation.Data>", as: Swift.Dictionary<Swift.Int,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Foundation.Data>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Bool_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Bool>", as: Swift.Dictionary<Swift.Int,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Bool>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Double_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Double>", as: Swift.Dictionary<Swift.Int,Swift.Double>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Double>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_Int_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.Int>", as: Swift.Dictionary<Swift.Int,Swift.Int>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.Int>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_String_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.String>", as: Swift.Dictionary<Swift.Int,Swift.String>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.String>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_Int_u002C_Swift_u002E_UInt8_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.Int,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.Int,Swift.UInt8>", as: Swift.Dictionary<Swift.Int,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.Int,Swift.UInt8>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Foundation_u002E_Data_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Foundation.Data>", as: Swift.Dictionary<Swift.String,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Foundation.Data>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Bool_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Bool>", as: Swift.Dictionary<Swift.String,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Bool>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Double_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Double>", as: Swift.Dictionary<Swift.String,Swift.Double>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Double>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_Int_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.Int>", as: Swift.Dictionary<Swift.String,Swift.Int>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.Int>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_String_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.String>", as: Swift.Dictionary<Swift.String,Swift.String>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.String>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_String_u002C_Swift_u002E_UInt8_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.String,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.String,Swift.UInt8>", as: Swift.Dictionary<Swift.String,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.String,Swift.UInt8>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Foundation_u002E_Data_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Foundation.Data> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Foundation.Data>", as: Swift.Dictionary<Swift.UInt8,Foundation.Data>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Foundation.Data>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Bool_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Bool> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Bool>", as: Swift.Dictionary<Swift.UInt8,Swift.Bool>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Bool>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Double_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Double> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Double>", as: Swift.Dictionary<Swift.UInt8,Swift.Double>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Double>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_Int_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.Int> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.Int>", as: Swift.Dictionary<Swift.UInt8,Swift.Int>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.Int>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_String_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.String> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.String>", as: Swift.Dictionary<Swift.UInt8,Swift.String>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.String>"))
}

@available(swift 4.0)
public func Host_Swift_u002E_Dictionary_u003C_Swift_u002E_UInt8_u002C_Swift_u002E_UInt8_u003E__values_instance(vm: inout VM, _ args: [Value]) throws -> Value {
    guard args.count == 1 else { throw VMError.typeError("Swift.Dictionary<Key,Value>.values expects 0 args") }
    guard case let .host(_, tid) = args[0] else { throw VMError.typeError("receiver must be Dictionary<Swift.UInt8,Swift.UInt8> host") }
    let receiver = try vm.withHost(args[0], typeName: "Dictionary<Swift.UInt8,Swift.UInt8>", as: Swift.Dictionary<Swift.UInt8,Swift.UInt8>.self) { host in
        host
    }
    let result = receiver.values
    return .host(HostRef(box: makeOpaqueBox(result)), vm.hostTypeID(named: "Dictionary<Swift.UInt8,Swift.UInt8>"))
}

@available(swift 4.0)
public func HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__values(vm: inout VM, _ args: [Value]) throws -> Value {
    guard !args.isEmpty else { throw VMError.typeError("missing receiver") }
    let tid: TypeID
    switch args[0] {
    case .host(_, let receiverTid): tid = receiverTid
    case .metatype(let receiverTid): tid = receiverTid
    default: throw VMError.typeError("receiver must be host or metatype")
    }
    if tid == vm.hostTypeID(named: "Dictionary<Foundation.Data,Foundation.Data>") {
        switch args[0] {
        case .host:
            if true { return try Host_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__values_instance(vm: &vm, args) }
            else { throw VMError.unsupported("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.values is not available on this platform") }
        default: break
        }
    }
    let receiverName = vm.debugTypeName(tid)
    let supported = [
    "  - Swift.Dictionary<Foundation.Data,Foundation.Data> ()",
] .joined(separator: "\n")
    throw VMError.typeError("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.values has no specialization for receiver \(receiverName). Supported specializations:\(supported)")
}

public func HostAvailWrap_HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__values(vm: inout VM, _ args: [Value]) throws -> Value {
    if true { return try HostDispatcher_Swift_u002E_Dictionary_u003C_Foundation_u002E_Data_u002C_Foundation_u002E_Data_u003E__values(vm: &vm, args) }
    throw VMError.unsupported("Getter for Swift.Dictionary<Foundation.Data,Foundation.Data>.values is not available on this platform")
}

